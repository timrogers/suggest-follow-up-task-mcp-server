#!/usr/bin/env node

const path = require('path');
const { spawn } = require('child_process');

// Get the directory of this script
const binDir = __dirname;
const projectRoot = path.resolve(binDir, '..');
const tsNodePath = path.join(projectRoot, 'node_modules', '.bin', 'ts-node');
const srcPath = path.join(projectRoot, 'src', 'index.ts');

// Run ts-node with the TypeScript source file
const child = spawn(tsNodePath, [srcPath], {
  stdio: 'inherit',
  cwd: projectRoot
});

child.on('exit', (code) => {
  process.exit(code);
});